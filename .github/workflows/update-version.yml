name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Inject version into index.html and service-worker.js
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          VERSION="v1.${{ github.run_number }}.${SHORT_SHA}"
          echo "Injecting version: $VERSION"
          sed -i "s|<div class=\"menu-version\">v[^<]*</div>|<div class=\"menu-version\">$VERSION</div>|g" index.html
          sed -i "s|neural-recon-v[^']*|neural-recon-$VERSION|g" service-worker.js

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

